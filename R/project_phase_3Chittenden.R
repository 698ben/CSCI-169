#library(tensorflow)
# library("keras")


# keras_model_sequential()
## Not run:
#library(keras)
print(5)
#library(keras, help, pos = 2, lib.loc = NULL)
#inputs <- layer_input(shape = c(784), sparse = FALSE, ragged = FALSE)
# predictions <- inputs
# layer_dense(units = 64, activation = 'relu') %>%
# layer_dense(units = 64, activation = 'relu') %>%
# layer_dense(units = 10, activation = 'softmax')
# create and compile model
# model <- keras_model(inputs = inputs, outputs = predictions)
# model %>% compile(
# optimizer = 'rmsprop',
# loss = 'categorical_crossentropy',
# metrics = c('accuracy')
#)